class Person {
    // attributes
    // it`s not needed to put 'let' or something else
    // before attribute`s declaration
    person_name:string;
    age:number;
    city:string
}


// create instance
let person = new Person();
